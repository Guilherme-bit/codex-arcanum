# URL do servidor multiplayer (Socket.IO).
# Vazio = mesma origem (recomendado quando tudo corre no Render).
# Exemplo para cliente na Vercel + servidor no Render:
# VITE_SERVIDOR_URL=https://codex-arcanum.onrender.com
VITE_SERVIDOR_URL=
